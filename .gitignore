__pycache__/
*.pyc
*.egg-info/
node_modules/
dist/
build/
.vite/
*.wasm
.env
.venv/
venv/

# Mocap binaries — big and often sensitive. Tracked test fixtures are
# kept via explicit exceptions below.
data/*.h5
!data/demo_with_nan.h5

# Per-machine symlinks pointing at species handoff packages outside the
# repo (e.g. data/stick → ~/stick_handoff/data).
data/stick
